# Backend environment example for Super App
# Never commit real secrets. Copy this file to .env and adjust values.

# Server
PORT=4000
HOST=0.0.0.0

# MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/super-app

# JWT (REQUIRED in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=please-change-me-to-a-long-random-string-at-least-32-chars
# Optional but recommended: separate secret for refresh tokens
REFRESH_JWT_SECRET=another-long-random-string-at-least-32-chars
# Optional: for rotation; when rotating, set previous here temporarily
# JWT_SECRET_PREVIOUS=

# Expirations (optional overrides)
# JWT_EXPIRES_IN=15m
# REFRESH_JWT_EXPIRES_IN=7d

# CORS (comma-separated origins for dev)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Development toggles
# Set to true to start server without connecting to MongoDB (development only)
SKIP_DB=false
